This is a simple file from a Yocto-built package!

This demonstrates how easy it is to add simple packages from Yocto to ISAR.
The package is built with Yocto and then added to the ISAR image via the local APT repository.

